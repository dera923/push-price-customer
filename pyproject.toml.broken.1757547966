[project]
name = "push-price-customer"
version = "poetry add numpy pandas scikit-learn matplotlib seaborn"
description = "因果推論による配信・価格施策最適化"
authors = [
    {name = "poetry add statsmodels scipy"}
]
license = {text = "poetry add plotly jupyter  # 可視化・検証用"}
readme = "README.md"
requires-python = "^3.9"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
